# fritzbrowser
A minimal Qt/KDE browser dedicated to your Fritz!Box web interface that logs you back in automatically before the box's ~20 minute inactivity timeout kicks in, so you never see the login screen again.
